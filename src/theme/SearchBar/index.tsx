import React from 'react';

/**
 * Custom SearchBar that disables Algolia search UI.
 * This removes the search input from the navbar and
 * avoids runtime errors from the default Algolia theme.
 */
export default function SearchBar(): JSX.Element | null {
  return null;
}

